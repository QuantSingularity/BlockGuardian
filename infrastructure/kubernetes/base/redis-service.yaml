apiVersion: v1
kind: Service
metadata:
  name: blockguardian-redis
  labels:
    app: blockguardian-redis
    component: cache
spec:
  type: ClusterIP
  ports:
    - port: 6379
      targetPort: 6379
      protocol: TCP
      name: redis
  selector:
    app: blockguardian-redis
    component: cache
