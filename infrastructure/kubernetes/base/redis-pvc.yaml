apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: blockguardian-redis-pvc
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: standard
  resources:
    requests:
      storage: 5Gi
